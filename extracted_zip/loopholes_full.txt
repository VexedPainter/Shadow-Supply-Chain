<USER_REQUEST>
Part 2: Loophole-by-Loophole Fix Specification
Each fix below is written so an AI coding assistant can implement it directly.

LP-01 — No Feedback Loop on Anomaly Detection
Current state: The shadow detection engine flags transactions with no matching PO. There is no mechanism to learn from human corrections. False positives are never used to recalibrate detection logic.

Exact fix to implement:

Add a feedback field to the shadow_purchases table:

sql
ALTER TABLE shadow_purchases ADD COLUMN reviewer_verdict TEXT; 
-- Values: 'confirmed_shadow', 'false_positive', 'needs_review'
ALTER TABLE shadow_purchases ADD COLUMN reviewed_at TIMESTAMP;
ALTER TABLE shadow_purchases ADD COLUMN reviewer_id TEXT;
Add a new API endpoint:

text
POST /api/shadows/{shadow_id}/feedback
Body: { "verdict": "false_positive", "reviewer_id": "tech_001" }
Add a recalibration job that runs every 24 hours:

python
# Count false_positive rate per vendor, category, amount_range
<truncated 28408 bytes>